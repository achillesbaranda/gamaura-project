{
  "version": 2,
  "builds": [
    {
      "src": "gamaura-project/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "gamaura-project/build"
      }
    }
  ],
  "routes": [
    {
      "src": "/static/(.*)",
      "headers": {
        "cache-control": "s-maxage=31536000,immutable"
      }
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}